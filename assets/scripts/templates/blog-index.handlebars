
{{#each blogs as |blog|}}
<div data-id="{{blog.id}}">
  <!-- Are we going to display id? -->
  <div class="handlebars-container">
    <h3 class="title" id="blog-title-display">{{blog.title}}</h3>
    <button class="btn btn-primary glyphicon glyphicon-menu-down" id="dropdown-blog" type="button" data-toggle="collapse" data-target="#collapseUpdate{{blog.id}}" aria-expanded="false" aria-controls="collapseUpdate{{blog.id}}">
</button>
<div class="collapse update-collapse" id="collapseUpdate{{blog.id}}">
  <div class="well">
    <form class="blogs" id="update-blog" data-id="{{blog.id}}">
      <div class="form-group">
        <label>Title
        <input type="text" name="blog[title]" maxlength="150" id="blog-title" placeholder="Title" value="{{blog.title}}">
      </label>
      <label>Author
        <input type="text" name="blog[author]" maxlength="150" id="blog-author" placeholder="Author" value="{{blog.author}}">
      </label>
        <br>
        <textarea rows="10" cols="50" id="blog-textarea" maxlength="10000" name="blog[content]" placeholder="Content">{{blog.content}}</textarea>
      </div>
      <button type="submit" id="update-blog-btn" class="btn btn-success glyphicon glyphicon-pencil" data-toggle="tooltip" data-placement="bottom" title="Edit"></button>
    </form>
    <button type="button" id="delete-blog" data-id="{{blog.id}}" class="btn btn-danger glyphicon glyphicon-trash" data-toggle="tooltip" data-placement="bottom" title="Delete"></button>
    <div id="clear-div"></div>
  </div>
  </div>
  <p class="author" id="blog-author-display">Written by: {{blog.author}}</p>
  <p class="date">Date created: {{blog.createdAt}}</p>
    <p class="content"><pre class="text-body">{{blog.content}}</pre></p>


  </div>

</div>


{{/each}}
